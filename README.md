Learnt about Collections

List array
  which is ordered because they have fixed position.


Map:
  Its an unordered collection of key-value pairs.
  Every value has a key
  Every key must be unique.
  Use a map when you need to access objects by a unique identifier.
  They are of type Map.